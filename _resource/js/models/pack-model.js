/**
 * description 打包目录的Model.
 *
 * @class app.model.PackModel
 */

namespace('app.model');

(function(){
    app.model.PackModel = Backbone.Model.extend({
        default: {
            modelName: 'PackModel'
        }


    });
})();