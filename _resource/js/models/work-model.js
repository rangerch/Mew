/**
 * description 工作目录的Model.
 *
 * @class app.model.WorkModel
 */

namespace('app.model');

(function(){
    app.model.WorkModel = Backbone.Model.extend({
        default: {
            modelName: 'WorkModel'
        }


    });
})();